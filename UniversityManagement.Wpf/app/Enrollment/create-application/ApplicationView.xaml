<UserControl x:Class="UniversityManagement.Wpf.Enrollment.ApplicationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:services="clr-namespace:UniversityManagement.Services.Enrollment;assembly=UniversityManagement.Services"
        xmlns:enrollment="clr-namespace:UniversityManagement.Wpf.Enrollment"
        mc:Ignorable="d"
        Height="450" Width="800">
    <UserControl.DataContext>
        <enrollment:ApplicationViewModel />
    </UserControl.DataContext>
    <StackPanel>
        <GroupBox Header="Applicant" Margin="6">
            <StackPanel>
                <Label Content="First Name:" />
                <TextBox Text="{Binding Path=ApplicantName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Label Content="Last Name:" />
                <TextBox Text="{Binding Path=ApplicantSurname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </GroupBox>
    </StackPanel>
</UserControl>
