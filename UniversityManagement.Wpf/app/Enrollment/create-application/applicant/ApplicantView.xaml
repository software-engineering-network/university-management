<UserControl x:Class="UniversityManagement.Wpf.Enrollment.ApplicantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <GroupBox Header="Applicant" Margin="6">
        <StackPanel>
            <Label Content="First Name:" />
            <TextBox Text="{Binding Path=ApplicantName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Last Name:" />
            <TextBox Text="{Binding Path=ApplicantSurname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </GroupBox>
</UserControl>
