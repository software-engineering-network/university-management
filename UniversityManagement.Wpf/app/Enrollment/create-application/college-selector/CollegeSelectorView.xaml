<UserControl x:Class="UniversityManagement.Wpf.Enrollment.CollegeSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:enrollment="clr-namespace:UniversityManagement.Wpf.Enrollment"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=enrollment:CollegeSelectorViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <ComboBox  
        ItemsSource="{Binding Path=Items}"
        SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay}"
        DisplayMemberPath="Text">
    </ComboBox>
</UserControl>
